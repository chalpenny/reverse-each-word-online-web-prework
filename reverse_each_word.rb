
#.each method
def reverse_each_word(sentence)
  # take in an argument of a sentence
  # convert to an array
  sentence.split
  # create new array to hold new data
  reversed = []
  # iterate across array using .each method to reverse each word
  sentence.each do |
  # convert back to a string
  # call new array
end

#.collect method
  # take in an argument of a sentence
  def reverse_each_word_easy(sentence_string)
  # convert to an array
  sentence_string.split
  # iterate across arra using .collect method to reverse each word

  # convert back to a string
end
